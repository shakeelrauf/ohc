%li.page-item{ class: "page#{' active' if page.current?}" }
  = link_to page, (page.current? ? 'javascript:void(0)' : url), opts = { class: 'page-link', remote: remote, rel: page.next? ? 'next' : page.prev? ? 'prev' : nil }
