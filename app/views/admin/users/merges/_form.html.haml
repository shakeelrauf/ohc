= f.select :source_id, @children.collect { |c| ["#{c.full_name} (#{c.email})", c.id]  },
           prompt: "Select #{model_name(User::Child)}",
           help: t('help.merge.source_id')

= f.select :destination_id, @children.collect { |c| ["#{c.full_name} (#{c.email})", c.id]  },
           prompt: "Select #{model_name(User::Child)}",
           help: t('help.merge.destination_id')
