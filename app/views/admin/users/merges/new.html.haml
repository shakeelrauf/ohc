- title_bar merge_button_label(User::Child)

= bootstrap_form_with url: users_merge_path, local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit merge_button_label(User::Child), class: 'btn btn-success', data: { confirm: 'Are you sure?' }
