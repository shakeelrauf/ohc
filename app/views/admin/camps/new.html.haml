- title_bar new_button_label(Camp)

= bootstrap_form_with model: @camp, local: true, url: season_camps_path(@camp.season) do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(Camp), class: 'btn btn-success', data: { disable_with: 'Uploading...' }
