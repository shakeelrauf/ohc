= render 'shared/form_base_errors', f: f

= f.text_field :name
= attachment_file_field(f, :video, help: t('help.file_field', file_type: 'MP4', file_size: number_to_human_size(WelcomeVideo::MAX_FILE_SIZE)).html_safe)

= f.form_group do
  = f.check_box :active
