- title_bar new_button_label(User::Admin)

= bootstrap_form_with model: @admin, url: admins_path, local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(User::Admin), class: 'btn btn-success'
