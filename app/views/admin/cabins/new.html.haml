- title_bar new_button_label(Cabin)

= bootstrap_form_with model: @cabin, local: true, url: camp_cabins_path(@cabin.camp) do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(Cabin), class: 'btn btn-success', data: { disable_with: 'Uploading...' }
