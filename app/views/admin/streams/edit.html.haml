- title_bar "Edit #{@stream.name}" do
  = delete_button(stream_path(@stream), text: "Delete #{@stream.name}", remote: false)

= bootstrap_form_with model: @stream do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit update_button_label(Stream), class: 'btn btn-success'
