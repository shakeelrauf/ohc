- title_bar new_button_label(CampLocation)

= bootstrap_form_with model: @camp_location, url: camp_locations_path, local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(CampLocation), class: 'btn btn-success'
