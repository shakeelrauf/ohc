- title_bar new_button_label(Season)

= bootstrap_form_with model: @season, url: camp_location_seasons_path(@season.camp_location), local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(Season), class: 'btn btn-success'
