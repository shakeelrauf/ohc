- title_bar new_button_label(ColorTheme)

= bootstrap_form_with model: @color_theme, url: color_themes_path, local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(ColorTheme), class: 'btn btn-success'
