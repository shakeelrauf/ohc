- title_bar new_button_label(User::Child)

= bootstrap_form_with model: @child, url: children_path, local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(User::Child), class: 'btn btn-success'
