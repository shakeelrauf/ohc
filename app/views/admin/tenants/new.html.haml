- title_bar new_button_label(Tenant)

= bootstrap_form_with model: @tenant, url: tenants_path, local: true do |f|
  = render 'form', f: f

  = f.form_group do
    = f.submit create_button_label(Tenant), class: 'btn btn-success'
